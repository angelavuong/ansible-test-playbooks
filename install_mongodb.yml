---
- name: Install MongoDB
  hosts: mongodb 
  gather_facts: false 
  become: true

  tasks: 
  - name: Install package mongodb-org
    ansible.builtin.package:
      name: mongodb-org
      state: present
      




